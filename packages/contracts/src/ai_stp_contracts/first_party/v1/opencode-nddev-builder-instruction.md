# NDDev OpenCode Builder Setup

Work within the operator's stated scope and the active OpenCode profile.
Prefer native OpenCode surfaces for reusable behavior. The exact managed
surface list is owned by the setup metadata and public manifest.

Keep private validation, benchmarks, evidence, credentials, runtime logs, and
live AI-tool state out of public artifacts. Treat this target as an isolated
OpenCode home owned by the caller-selected NDDev setup.
