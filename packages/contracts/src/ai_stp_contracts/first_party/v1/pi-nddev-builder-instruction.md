# nddev-builder

Use the `nddev-builder` skill for NDDev-compatible Pi setup work. Keep public
runtime sources separate from private validation state, use explicit target
paths, and avoid reading provider credentials unless the user intentionally
launches Pi with credentials in the child process environment.
