{
  "$id": "urn:ai-stp:schema:v1:runtime-usage-record-result",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "description": "The outcome of recording one accepted invocation into the outbox.\n\n`queued` and `duplicate` are the durable states; `full` and `dropped`\nmean the local queue could not accept the event - the caller treats them\nas non-fatal telemetry loss, never as a reason to fail the invocation.",
  "properties": {
    "event_id": {
      "title": "Event Id",
      "type": "string"
    },
    "schema_version": {
      "const": 1,
      "default": 1,
      "title": "Schema Version",
      "type": "integer"
    },
    "state": {
      "enum": [
        "queued",
        "duplicate",
        "full",
        "dropped"
      ],
      "title": "State",
      "type": "string"
    }
  },
  "required": [
    "event_id",
    "schema_version",
    "state"
  ],
  "title": "RuntimeUsageRecordResult",
  "type": "object"
}
