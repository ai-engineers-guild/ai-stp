{
  "$id": "urn:ai-stp:schema:v1:cli-provider-network-capability",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "description": "The observed network boundary on this exact machine, for both protocols.\n\nThe report never turns absence into support. Evidence names the launcher\nversion/digest and transport probes when enforcement is observed; an\nunavailable result remains actionable machine data rather than a log line.\n\nIt answered for protocol v2 alone until 2026-08-27, and v2 is not what\nanything installs with. `#416` allows a **v3** local phase to run with no\nnetwork-denying launcher on a platform that has none, gated on a trusted\nrelease or an explicit `--unverified-provider` — deliberate security debt,\nand this is the one output someone would check to find it. Describing only\nv2 meant the marker pointed at a protocol nobody runs.",
  "properties": {
    "evidence": {
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "title": "Evidence",
      "type": "array"
    },
    "launcher_id": {
      "default": "",
      "title": "Launcher Id",
      "type": "string"
    },
    "local_actions_available": {
      "title": "Local Actions Available",
      "type": "boolean"
    },
    "network_enforcement": {
      "enum": [
        "enforced",
        "unavailable"
      ],
      "title": "Network Enforcement",
      "type": "string"
    },
    "os_name": {
      "minLength": 1,
      "title": "Os Name",
      "type": "string"
    },
    "protocol_version": {
      "const": 2,
      "default": 2,
      "title": "Protocol Version",
      "type": "integer"
    },
    "schema_version": {
      "const": 1,
      "default": 1,
      "title": "Schema Version",
      "type": "integer"
    },
    "v3_local_phase": {
      "enum": [
        "network_denied",
        "unisolated_by_trust",
        "refused"
      ],
      "title": "V3 Local Phase",
      "type": "string"
    },
    "v3_local_phase_reasons": {
      "default": [],
      "items": {
        "type": "string"
      },
      "title": "V3 Local Phase Reasons",
      "type": "array"
    }
  },
  "required": [
    "evidence",
    "launcher_id",
    "local_actions_available",
    "network_enforcement",
    "os_name",
    "protocol_version",
    "schema_version",
    "v3_local_phase",
    "v3_local_phase_reasons"
  ],
  "title": "ProviderNetworkCapability",
  "type": "object"
}
