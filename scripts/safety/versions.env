# Pinned scanner tool versions for worker-safety image builds.
# Bump deliberately; record the reason in the PR that changes pins.
# Verified against GitHub/PyPI release assets on 2026-08-12.

GITLEAKS_VERSION=8.30.1
OPENGREP_VERSION=1.26.0
OSV_SCANNER_VERSION=2.5.0
SHELLCHECK_VERSION=0.11.0
YARA_VERSION=4.5.2
# Python tools installed with uv into /opt/safety-venv (not the app venv)
BANDIT_VERSION=1.9.4
PIP_AUDIT_VERSION=2.10.1
# Go tools: gosec from GitHub release tarball; govulncheck via go install (required)
GOSEC_VERSION=2.28.0
GOVULNCHECK_VERSION=v1.1.4
# Skill engines (required in worker-safety): NVIDIA SkillSpector from git tag +
# Cisco skill-scanner from PyPI package cisco-ai-skill-scanner (CLI: skill-scanner).
SKILLSPECTOR_GIT_URL=https://github.com/NVIDIA/skillspector.git
SKILLSPECTOR_GIT_REF=v2.9.3
SKILL_SCANNER_PACKAGE=cisco-ai-skill-scanner
SKILL_SCANNER_VERSION=2.0.13
# ClamAV comes from distro packages; pin the apt package set in Dockerfile.
