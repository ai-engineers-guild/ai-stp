---
title: "skill"
description: "Skill-компоненты: повторяемые workflows для агента."
---

# `skill`

`skill` — переносимый агентский workflow. Обычно он содержит `SKILL.md`,
инструкции, references, assets и иногда scripts, которые помогают агенту
выполнить специализированную задачу без угадывания процесса.

Skill отвечает на вопрос: “как агент должен делать этот класс задач?”

## Чем skill отличается от соседних видов

| Вид | Главное отличие |
| --- | --- |
| `instruction` | даёт общие правила и контекст, но не обязательно описывает workflow |
| `command` | запускается как именованный shortcut, а skill активируется по смыслу задачи |
| `plugin` | расширяет сам харнесс, а skill расширяет рабочее поведение агента |
| `mcp` | даёт инструментальный интерфейс, а skill объясняет, когда и как им пользоваться |

## Как skill проходит через `ai_stp`

=== "Автор"
    Автор публикует skill из публичного GitHub-источника или импортирует
    локально. Версия фиксирует точный commit и подпуть.

=== "Каталог"
    Каталог показывает назначение, поддерживаемые харнессы, ограничения,
    trusted status автора и independent status самого компонента.

=== "Сборщик"
    Сборщик проверяет, что skill можно встроить в выбранный сетап и что его
    файловая структура подходит provider-проекции.

=== "Provider"
    Provider кладёт skill в нативный каталог харнесса и обновляет связанные
    индексы только после плана, digest и подтверждения.

## Что смотреть перед установкой

| Проверка | Почему важно |
| --- | --- |
| Есть ли scripts | скрипты могут выполнять действия вне Markdown |
| Есть ли references/assets | агенту понадобится весь комплект, не только `SKILL.md` |
| Совместим ли харнесс | одинаковое название skill не гарантирует одинаковый формат |
| Кто автор | verified-автор не делает содержимое автоматически безопасным |
| Какая версия закреплена | обновление skill создаёт новую версию сетапа |

??? question "Можно ли skill использовать без публикации"
    Да. Собственный, импортированный или точно pinned skill можно использовать
    после локальных проверок. Но он не становится platform-verified и должен
    отображаться именно как локальный или закреплённый объект.
