# Единый перевод строки в репозитории. Без этого клон на Windows даёт CRLF,
# и любой инструмент, читающий файлы построчно, начинает ругаться, а diff
# показывает изменённым весь файл.
* text=auto eol=lf

# Двоичные файлы не трогаем.
*.png binary
*.jpg binary
*.pdf binary
*.ico binary
