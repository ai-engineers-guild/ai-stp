---
title: Setups
description: Complete configurations pinned to one harness and exact component versions.
---

A setup belongs to exactly one harness. Its version pins every component version, so any composition change creates a new setup version.

Before installation, check the trust lane, required credentials, authorization requirements, license, and conversion report.
