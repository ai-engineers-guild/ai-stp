# Generated by MSW's postinstall (`msw init`), not authored here. Formatting it
# would be undone by the next `bun install`, so the format gate skips it instead
# of fighting the generator every time the dependency moves.
public/mockServiceWorker.js

# Contract projections generated from packages/contracts. back-static compares
# them to the Python renderer; Prettier would fight that gate.
src/lib/generated/cli-copy.ts
src/lib/generated/deep-link-corpus.ts

# Build output.
.next/
node_modules/
coverage/
