rules:
  - id: github-actions-pull-request-target-checkout
    message: untrusted workflow trigger with checkout can expose secrets to untrusted code.
    severity: WARNING
    languages: [yaml]
    # nosemgrep: repo-safety.opengrep.github-actions-pull-request-target-checkout
    pattern-regex: 'pull_request_target'

  - id: github-actions-curl-bash
    message: Avoid curl|bash installers in CI; pin official actions or verified checksums.
    severity: ERROR
    languages: [yaml]
    # nosemgrep: repo-safety.opengrep.github-actions-curl-bash
    pattern-regex: 'curl.+\|\s*(bash|sh)'
